{% extends 'base.html.twig' %}

{% block title %}Hello MainDashboardController!{% endblock %}

{% block body %}
<div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold text-gray-800 mb-6">Dashboard</h1>
    
    {# Usar dados do controller #}
    <div {{ react_component('Hello', { 'fullName': user_data.name }) }}></div>
    
    {# The React component will stay mounted if the div is moved in the DOM #}
    <div class="mt-8 bg-white rounded-lg shadow-lg">
        {{ render_chart(chart) }}
    </div>
</div>
{% endblock %}
